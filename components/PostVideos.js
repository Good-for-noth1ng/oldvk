import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const PostVideos = ({postVideos}) => {
  return (
    <View>
      <Text>PostVideos</Text>
    </View>
  )
}

export default PostVideos

const styles = StyleSheet.create({})